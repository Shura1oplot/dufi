--- pygubu.orig/widgets/editabletreeview.py
+++ pygubu/widgets/editabletreeview.py
@@ -270,8 +270,8 @@
         if col not in self._inplace_widgets:
             self._inplace_widgets[col] = tk.Spinbox(self,
                 textvariable=svar, from_=min, to=max, increment=step)
-        sb = self._inplace_widgets[col]
-        sb.bind('<Unmap>', lambda e: self.__update_value(col, item))
+        cb = self._inplace_widgets[col]
+        cb.bind('<Unmap>', lambda e: self.__update_value(col, item))
         cb.bind('<FocusOut>', lambda e: self.__update_value(col, item))
         self._inplace_widgets_show[col] = True

--- pygubu.orig/widgets/tkscrollbarhelper.py
+++ pygubu/widgets/tkscrollbarhelper.py
@@ -23,6 +23,7 @@
 except:
     import Tkinter as tk
 from pygubu import ApplicationLevelBindManager as BindManager
+from pygubu.binding import remove_binding


 logger = logging.getLogger(__name__)
@@ -130,7 +131,7 @@
                                        add='+')
                     self._bindingids.append((cwidget, bid))
                 for s in (self.vsb, self.hsb):
-                    if s:
+                    if s and s != main_sb:
                         bid = s.bind('<Enter>',
                                      lambda event, scrollbar=s: BindManager.mousewheel_bind(scrollbar),
                                      add='+')
--- pygubu.orig/widgets/tkscrolledframe.py
+++ pygubu/widgets/tkscrolledframe.py
@@ -23,6 +23,7 @@
 except:
     import Tkinter as tk
 from pygubu import ApplicationLevelBindManager as BindManager
+from pygubu.binding import remove_binding


 def ScrolledFrameFactory(frame_class, scrollbar_class):
@@ -290,7 +291,7 @@
                                     add='+')
                     self._bindingids.append((self, bid))
                 for s in (self.vsb, self.hsb):
-                    if s:
+                    if s and s != main_sb:
                         bid = s.bind('<Enter>',
                                      lambda event, scrollbar=s: BindManager.mousewheel_bind(scrollbar),
                                      add='+')
